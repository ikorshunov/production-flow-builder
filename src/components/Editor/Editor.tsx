import { Background, Controls, ReactFlow } from '@xyflow/react';

export const Editor = () => {
    return (
        <ReactFlow>
            <Background />
            <Controls />
        </ReactFlow>
    );
};
